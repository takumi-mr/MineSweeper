import java.util.Random;
public class generate {
     Random rnd =  new Random();
     public static void generate_Bomb(int[][][] x,int numOfMine) {
        for (int i = 0; i < numOfMine; i++) {
        }
    }
}
